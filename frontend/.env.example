# Local development default
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel frontend example:
# NEXT_PUBLIC_API_URL=https://complyscan-api.vercel.app
#
# The hosted UI automatically caps each image at 4,000,000 bytes so each
# per-image multipart request remains below Vercel's function payload limit.
