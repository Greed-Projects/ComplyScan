[project]
name = "complyscan-api"
version = "0.8.0"
requires-python = ">=3.13,<3.14"

[tool.pytest.ini_options]
addopts = "--strict-config"
pythonpath = ["."]
testpaths = ["tests"]
filterwarnings = [
    "error::DeprecationWarning",
    "error::FutureWarning",
]
